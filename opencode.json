{
  "$schema": "https://opencode.ai/config.json",
  "command": {
    "push": {
      "description": "Check code quality, select a branch, and push to origin.",
      "template": "git-helper"
    }
  }
}
